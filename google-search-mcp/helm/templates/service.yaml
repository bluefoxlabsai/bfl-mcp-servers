apiVersion: v1
kind: Service
metadata:
  name: {{ include "google-search-mcp-server.fullname" . }}
  labels:
    {{- include "google-search-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google-search-mcp-server.selectorLabels" . | nindent 4 }}