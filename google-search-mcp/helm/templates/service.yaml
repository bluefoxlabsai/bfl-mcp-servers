{{- $serviceEnabled := false }}
{{- if kindIs "string" .Values.service.enabled }}
  {{- if eq .Values.service.enabled "auto" }}
    {{- if ne .Values.mcp.transport "stdio" }}
      {{- $serviceEnabled = true }}
    {{- end }}
  {{- else if eq .Values.service.enabled "true" }}
    {{- $serviceEnabled = true }}
  {{- end }}
{{- else if .Values.service.enabled }}
  {{- $serviceEnabled = true }}
{{- end }}
{{- if $serviceEnabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "google-search-mcp.fullname" . }}
  labels:
    {{- include "google-search-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google-search-mcp.selectorLabels" . | nindent 4 }}
{{- end }}