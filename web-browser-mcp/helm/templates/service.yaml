apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-browser-mcp.fullname" . }}
  labels:
    {{- include "web-browser-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-browser-mcp.selectorLabels" . | nindent 4 }}