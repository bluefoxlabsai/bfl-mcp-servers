# =============================================================================
# SLACK TOKEN CONFIGURATION
# =============================================================================

# Slack Bot Token (starts with xoxb-)
# Required for most API calls including posting messages, reading channels, etc.
#
# HOW TO GENERATE:
# 1. Go to https://api.slack.com/apps
# 2. Click "Create New App" > "From scratch"
# 3. Enter app name (e.g., "MCP Server") and select your workspace
# 4. Go to "OAuth & Permissions" in the left sidebar
# 5. Scroll down to "Scopes" and add these Bot Token Scopes:
#    - channels:read (list public channels)
#    - channels:history (read message history)
#    - chat:write (post messages)
#    - reactions:write (add reactions)
#    - users:read (get user information)
#    - files:read (access file information)
# 6. Click "Install to Workspace" at the top
# 7. Copy the "Bot User OAuth Token" (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here

# Slack User Token (starts with xoxp-)  
# Required for search functionality and some advanced features
#
# HOW TO GENERATE:
# 1. In the same app at https://api.slack.com/apps
# 2. Go to "OAuth & Permissions" in the left sidebar
# 3. Scroll down to "User Token Scopes" and add:
#    - search:read (search messages and files)
#    - channels:read (list channels user has access to)
#    - users:read (get user information)
# 4. Click "Install to Workspace" (you may need to reinstall)
# 5. Copy the "User OAuth Token" (starts with xoxp-)
#
# IMPORTANT: User tokens have more permissions and should be handled carefully.
# The user who installs the app will be the one whose permissions are used.
SLACK_USER_TOKEN=xoxp-your-slack-user-token-here

# =============================================================================
# ADDITIONAL CONFIGURATION
# =============================================================================

# Optional: Enable safe search mode to exclude private channels and DMs from search results
# Set to "true" to enable, "false" or unset to disable
# Recommended: true for production environments to respect privacy
SLACK_SAFE_SEARCH=false

# =============================================================================
# TESTING TOKENS (Optional)
# =============================================================================

# Examples for testing (used by example scripts)
# These can be the same as the tokens above, or separate tokens for testing
EXAMPLES_SLACK_BOT_TOKEN=xoxb-your-slack-token-here
EXAMPLES_SLACK_USER_TOKEN=xoxp-your-slack-token-here

# =============================================================================
# QUICK SETUP CHECKLIST
# =============================================================================
#
# □ 1. Create Slack app at https://api.slack.com/apps
# □ 2. Add Bot Token Scopes: channels:read, channels:history, chat:write, 
#      reactions:write, users:read, files:read
# □ 3. Add User Token Scopes: search:read, channels:read, users:read
# □ 4. Install app to workspace
# □ 5. Copy Bot Token (xoxb-) to SLACK_BOT_TOKEN above
# □ 6. Copy User Token (xoxp-) to SLACK_USER_TOKEN above
# □ 7. Copy this file to .env and update with your actual tokens
# □ 8. Test with: uv run slack-mcp-server --port 8000
#
# SECURITY NOTE: Never commit your actual tokens to version control!
# The .env file is in .gitignore to prevent accidental commits.
#
# =============================================================================