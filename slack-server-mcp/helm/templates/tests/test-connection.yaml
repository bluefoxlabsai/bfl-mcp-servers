apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slack-server-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "slack-server-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "slack-server-mcp.fullname" . }}:{{ .Values.mcp.server.port }}/health']